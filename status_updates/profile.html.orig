<!DOCTYPE html>
<html>
<head>
	<title>Fecesbook</title>
	
	<script>
	function statusUpdate()
	{
		//First, get the message and date/time
		var clearField = document.getElementById("msg");
		var message = document.getElementById("msg").value;
		var date = new Date();
		var month = date.getMonth() + 1;
		var day = date.getDate();
		var year = date.getFullYear();
		var hour = date.getHours();
		var minute = date.getMinutes();
		if (minute < 10) {
			minute = "0" + minute;
		}
		var goodDate = month + "/" + day + "/" + year + " " + 
			hour + ":" + minute;
		var toPrint = goodDate + " - " + message;
		//Now, create paragraph that will be added
		var paragraph = document.createElement("p");
		var update = document.createTextNode(toPrint);

		paragraph.appendChild(update);

		//Finally, add the update at the top of the "status" div
		document.getElementById("status").insertBefore(paragraph, 
			document.getElementById("status").firstChild);
<<<<<<< HEAD

		function reload(){
			elem = document.getElementById("results");
			output = "";
			for (key in localStorage) {
				output = output + "<p>" + localStorage[key] + "</p>\n";
			}
			elem.innerHTML = output;
		}
	
		function store()
		{
			txt = document.getElementById("something").value;
			localStorage[new Date().getTime()] = txt;
			reload();
		}
		}
=======
		localStorage[new Date().getTime()] = toPrint;
		reload();
		clearField.value = "";
	}

	function reload()
	{
		elem = document.getElementById("status");
		output = "";
		for (key in localStorage) {
			output = "<p>" + localStorage[key] + "</p>\n" + output;
		}
		elem.innerHTML = output;
	}
>>>>>>> master
	</script> 


</head>

<body onload="reload()">
	<h1>Fecebook</h1>
	<div id="nav">
		<ul>
			<li id="home">Home</li>
			<li id="groups">Groups</li>
			<li id="profile">Profile</li>
			<li id="contacts">Contacts</li>
			<li id="inbox">Inbox</li>
			<li id="applications">Applications</li>
		</ul>
	</div>
	<h2 id="fullname">Barney Gumble</h2>
	<h3>What are you thinking about? <input type="text" id="msg" name="msg" onchange="statusUpdate()" size="30" /></h3>
	<h3>Updates</h3>
	<div id="status">
	</div>
	<div id="yesterday">
		<h3>Updates from yesterday:</h3>
		<ul>
			<li>Alice is now connected to Bob</li>
			<li>Tom recommends Jerry</li>
			<li>Harry just returned from a trip from Curaco</li>
		</ul>
	</div>
	<div id="longlongtimeago">
		<h3>Updates from long long time ago:</h3>
		<ul>
			<li>Harry just returned from a trip from Curaco</li>
			<li>Joan is now connected to Mary</li>
			<li>Steven is now connected to James</li>
		</ul>
	</div>
</body>
</html>
